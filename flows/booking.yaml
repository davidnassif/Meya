name: booking
states:
    text_buttons_state:
        component: meya.text_buttons
        properties:
            text: هل أنت مهتم في حجز
            output: button_click
            buttons:
                - text: زيارة واحدة
                  action: next
                  flow: SingleVisitEN
                  data:
                    foo: bar
                - text: باقة
                  action: next
                  flow: PackageEN
        delay:
            relative: 5